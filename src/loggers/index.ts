import beforeFilter from "./beforeFilter";
import beforeReduce from "./beforeReduce";
import onEachRuleCheck from "./onEachRuleCheck";
import beforeResolve from "./beforeResolve";
import afterResolve from "./afterResolve";

export default {
  beforeFilter,
  onEachRuleCheck,
  beforeReduce,
  beforeResolve,
  afterResolve,
}
